const config = {
  siteName: process.env.SITE_NAME || "Dòng họ Phạm Hữu",
  exampleEmail: process.env.EXAMPLE_EMAIL || "",
  examplePassword: process.env.EXAMPLE_PASSWORD || "",
  demoDomain: process.env.DEMO_DOMAIN || "https://phamhuu.vercel.app/",
};

export default config;
